import 'package:flutter/material.dart';

class DeviceConnection extends StatelessWidget {
  const DeviceConnection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('DeviceConnection');
  }
}

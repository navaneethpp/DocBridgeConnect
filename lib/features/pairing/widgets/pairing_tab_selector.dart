import 'package:flutter/material.dart';

class PairingTabSelector extends StatelessWidget {
  const PairingTabSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('PairingTabSelector');
  }
}

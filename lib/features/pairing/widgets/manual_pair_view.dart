import 'package:flutter/material.dart';

class ManualPairView extends StatelessWidget {
  const ManualPairView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('ManualPairView');
  }
}

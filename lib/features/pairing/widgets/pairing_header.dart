import 'package:flutter/material.dart';

class PairingHeader extends StatelessWidget {
  const PairingHeader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Pairing Header');
  }
}
